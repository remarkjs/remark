{
  "name": "remark.com",
  "private": true,
  "version": "0.0.0",
  "description": "The remark.com website",
  "author": "Titus Wormer <tituswormer@gmail.com>",
  "dependencies": {},
  "devDependencies": {
    "rehype-cli": "^3.0.0",
    "rehype-format": "^2.1.0",
    "rehype-highlight": "^2.0.0",
    "stylelint": "^8.0.0",
    "stylelint-config-standard": "^17.0.0",
    "xo": "^0.18.2"
  },
  "scripts": {
    "build:html": "rehype -u highlight -u format src/index.html -o index.html",
    "build": "npm run build:html",
    "lint": "xo && stylelint index.css",
    "test": "npm run build && npm run lint"
  },
  "stylelint": {
    "extends": "stylelint-config-standard"
  },
  "xo": {
    "space": true,
    "esnext": false
  }
}
