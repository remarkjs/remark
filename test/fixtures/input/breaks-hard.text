These are not breaks:

Look at the
pretty line
breaks.

These are breaks:

Look at the  
pretty line  
breaks.

In `commonmark: true` mode, an escaped newline character is exposed as a `break` node:

Look at the\
pretty line\
breaks.
