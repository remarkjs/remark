This example checks that [^the generated] IDs do not overwrite the user's IDs[^footnote-1].

[^footnote-1]: Old behavior would, for "generated", generate a footnote with an ID set to `footnote-1`, thus overwriting this footnote.
